import 'package:flutter/material.dart';
import 'package:mpax_flutter/widgets/util_widgets.dart';

class MediaItemMenu extends StatelessWidget {
  const MediaItemMenu({super.key});

  @override
  Widget build(BuildContext context) {
    return ModalDialog(
      child: Column(
        children: [],
      ),
    );
  }
}
